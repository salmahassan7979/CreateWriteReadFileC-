# CreateWriteReadFileC-
 Using the desktop app using C # you can click a button to create a file and there is another button to control writing inside the file and another button to read inside the file
